// Initialize angular module
var app = angular.module('blogs', []);

// Angular controller for the main content window
app.controller('BlogController', function($scope) {

    $scope.blogs = [1, 2, 3, 4, 5];

    

});
